Put recordings here. The directory is gitignored, this file is not.

Anything in here is a keystroke log — the text you typed can be reconstructed
from it — so it stays local. Point the analyzer at it:

    python3 ../analyze.py recordings/typing-2026-08-18-16-17-10.json
